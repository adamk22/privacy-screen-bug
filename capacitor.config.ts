import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todaytomorrow.testapp',
  appName: 'test',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
